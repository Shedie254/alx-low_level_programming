#include "main.h"

/**
  * _abs - it computes the absolute value of a number
  *
  * @j: the integer input
  *
  * Return: the absolute value of j
  */
int _abs(int j);
{
	if (j < 0)
		return (-j);
	else
		return (j);
}
